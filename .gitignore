# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
env/

# Environment / secrets — NEVER commit these
.env
.env.local
*.pem
*.key

# Google OAuth — credentials file and generated token
# Store these outside the repo (e.g. ~/.config/merv/) and point to them via .env
google_credentials.json
google_token.json
*credentials*.json
*token*.json
service_account*.json

# Merv data dir if ever placed inside the repo
data/
*.memory.json

# Test artifacts
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Logs
*.log
logs/
